apiVersion: v1
kind: Service
metadata:
  name: udagram-frontend
  labels:
    run: udagram-frontend
spec:
  ports:
    - port: 8100
      protocol: TCP
  type: ClusterIP
  selector:
    run: udagram-frontend